import crypto from 'node:crypto';import {createAccess,getJson,updateAccess,configured} from './_store.js';import {json,safeEqual} from './_security.js';import {sendAccessEmail} from './_email.js';
function signatureValid(req){const secret=process.env.KIWIFY_WEBHOOK_SECRET;if(!secret)return false;const received=String(req.headers['x-kiwify-signature']||req.headers['x-webhook-signature']||req.query?.token||'');if(req.query?.token&&safeEqual(received,secret))return true;const expected=crypto.createHmac('sha256',secret).update(JSON.stringify(req.body||{})).digest('hex');return safeEqual(received.replace(/^sha256=/,''),expected);}
function val(o,paths){for(const p of paths){let x=o;for(const k of p.split('.'))x=x?.[k];if(x!==undefined&&x!==null&&x!=='')return x;}return '';}
export default async function handler(req,res){if(req.method!=='POST')return json(res,405,{error:'Método não permitido'});if(!configured())return json(res,503,{error:'Storage pending'});if(!signatureValid(req))return json(res,401,{error:'Assinatura inválida'});try{const event=String(val(req.body,['event','event_type','type','order_status','status'])).toLowerCase();const product=String(val(req.body,['product.name','Product.product_name','product_name','offer.product.name']));const allowed=(process.env.KIWIFY_PLUS_PRODUCT_MATCH||'ontop').toLowerCase().split(',').map(x=>x.trim()).filter(Boolean);if(product&&!allowed.some(x=>product.toLowerCase().includes(x)))return json(res,200,{ok:true,ignored:'product'});const orderId=String(val(req.body,['order_id','order.id','sale_id','id','transaction_id']));const email=String(val(req.body,['customer.email','Customer.email','buyer.email','email']));const name=String(val(req.body,['customer.name','Customer.full_name','buyer.name','name']));const paid=['paid','approved','compra_aprovada','purchase_approved','order_approved'].some(x=>event.includes(x));const revoked=['refunded','refund','chargeback','cancelled','canceled','reembolso'].some(x=>event.includes(x));if(paid){const prior=orderId?await getJson(`order:${orderId}`):null;if(prior){const existing=await getJson(`access:${prior.code}`);return json(res,200,{ok:true,code:prior.code,duplicate:true,emailSent:Boolean(existing?.delivery?.sent)});}const access=await createAccess({email,name,orderId,source:'kiwify'});let delivery={sent:false};try{delivery=await sendAccessEmail({email,name,code:access.code,origin:process.env.APP_ORIGIN||`https://${req.headers.host}`});await updateAccess(access.code,{delivery});}catch(error){await updateAccess(access.code,{delivery:{sent:false,error:String(error.message)}});}return json(res,201,{ok:true,code:access.code,emailSent:delivery.sent});}if(revoked&&orderId){const prior=await getJson(`order:${orderId}`);if(prior)await updateAccess(prior.code,{status:'blocked',blockedReason:event});return json(res,200,{ok:true,revoked:Boolean(prior)});}return json(res,200,{ok:true,ignored:'event'});}catch(e){console.error(e);return json(res,500,{error:'Webhook processing failed'});}}
