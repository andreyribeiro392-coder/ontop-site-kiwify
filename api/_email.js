const appsScriptPattern=/^https:\/\/script\.google\.com\/macros\/s\/[A-Za-z0-9_-]+\/exec$/;
export function emailConfiguration(){const appsScriptUrl=String(process.env.APPS_SCRIPT_URL||'').trim();const appsScriptSecret=Boolean(process.env.APPS_SCRIPT_SECRET);const resendKey=Boolean(process.env.RESEND_API_KEY);const resendFrom=Boolean(process.env.ACCESS_FROM_EMAIL);return {provider:appsScriptUrl&&appsScriptSecret?'google-apps-script':resendKey&&resendFrom?'resend':'none',appsScript:{urlConfigured:Boolean(appsScriptUrl),secretConfigured:appsScriptSecret,urlValid:appsScriptPattern.test(appsScriptUrl)},resend:{keyConfigured:resendKey,senderConfigured:resendFrom}};}
export async function sendAccessEmail({email,name,code,origin}){if(!email)return {sent:false,reason:'EMAIL_MISSING'};if(process.env.APPS_SCRIPT_URL&&process.env.APPS_SCRIPT_SECRET){const url=String(process.env.APPS_SCRIPT_URL).trim();if(!appsScriptPattern.test(url))throw new Error('APPS_SCRIPT_URL_INVALID');const response=await fetch(url,{method:'POST',redirect:'follow',signal:AbortSignal.timeout(20000),headers:{'Content-Type':'application/json'},body:JSON.stringify({secret:process.env.APPS_SCRIPT_SECRET,email,name,code,origin})});const text=await response.text();let result={};try{result=JSON.parse(text)}catch{}if(!response.ok)throw new Error(`APPS_SCRIPT_EMAIL_${response.status}`);if(result.ok!==true)throw new Error(`APPS_SCRIPT: ${String(result.erro||result.error||'Resposta inválida').slice(0,160)}`);return {sent:true,provider:'google-apps-script'};}if(!process.env.RESEND_API_KEY||!process.env.ACCESS_FROM_EMAIL)return {sent:false,reason:'EMAIL_NOT_CONFIGURED'};const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.ACCESS_FROM_EMAIL,to:[email],subject:'Seu acesso ao OnTop Central Plus',html:`<div style="font-family:Arial;background:#08090c;color:#f5f6f8;padding:32px"><div style="max-width:560px;margin:auto;background:#121419;border:1px solid #513b18;border-radius:22px;padding:30px"><p style="color:#f2b84b;font-weight:800">ONTOP CENTRAL PLUS</p><h1>Seu acesso foi liberado</h1><p>Olá, ${escapeHtml(name||'membro Plus')}.</p><p>Use seu código individual:</p><div style="font-size:24px;font-weight:900;color:#f2b84b">${escapeHtml(code)}</div><p><a href="${origin}">Acessar Central Plus</a></p></div></div>`})});if(!response.ok)throw new Error(`EMAIL_${response.status}`);return {sent:true,provider:'resend'};}
function escapeHtml(value){return String(value).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
